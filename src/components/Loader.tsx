import { NewtonsCradle } from '@uiball/loaders';

const Loader = () => (
  <div className="flex justify-center">
    <NewtonsCradle size={40} speed={1.4} color="#666666" />
  </div>
);

export default Loader;
